spring:
  cloud:
    config:
      uri: ${CONFIG_SERVER_URI}
      name: gateway-service

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_SERVER_URI}

management:
  endpoints:
    web:
      exposure:
        include: bus-refresh
